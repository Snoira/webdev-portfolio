<section class="hero" id="hero">
        <h1>
            Hello! I’m
            <i class="accent-font">Nora</i>,
            <br>
            a
            <i class="accent-font">curious</i>
            junior fullstack developer
        </h1>
        <p>
            Who also enjoys playing around with
            <span class="bold">shapes</span>,
            <span class="color">colors</span> and
            <i class="accent-font">fonts</i>.
        </p>
</section>
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    .accent-font {
        font-family: "Libre Baskerville", Georgia, serif;
        font-weight: 400;
    }
    .bold {
        font-weight: 900;
    }
    .color {
        color: #ff6600;
        color: #A4D21D;
    }

    h1 {
        font-family: "Libre Franklin", Arial, Helvetica, sans-serif;
        font-weight: 700;
        font-size: clamp(2rem, 6vw, 4rem);
        line-height: 1.2;
        text-align: center;
    }
    p {
        font-family: "Libre Franklin", Arial, Helvetica, sans-serif;
        font-weight: 400;
        font-size: clamp(1.25rem, 3.7vw, 2.25rem);
        line-height: 1.5;
        text-align: center;
        color: #757575;
    }
    .hero {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: 100px 5%;
        /* height: 70dvh; */
    }

    @media (min-width: 700px) {
        .hero {
            padding: 150px 10%;
            align-items: flex-start;
        }
        h1 {
            /* font-size: clamp(3rem, 5vw, 5rem); */
            text-align: left;
        }
        p {
            /* font-size: clamp(1.5rem, 2.5vw, 2.5rem); */
            text-align: left;
        }
    }
</style>
