<section class="hero" id="hero">
    <div class="text-container">
        <h1 class="text">Hello! I’m Nora</h1>
        <p class="text">
            A junior full-stack JavaScript developer looking for new
            opportunities.
        </p>
    </div>
    <div class="projects-intro">
        <p class="color accent-font projects-text">
            <i>Take a look at some of my projects!</i>
        </p>
        <svg
            class="arrow-down"
            aria-hidden="true"
            xmlns="http://www.w3.org/2000/svg"
            width="70"
            fill="currentColor"
            viewBox="0 0 50 80"
        >
            <path
                stroke="currentColor"
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="1"
                d="M 24.5,5 
            L 25.5,5 
            C 30.5,30 31,45.5 26,71.5 
            L 40,50 
            Q 27,76.5 24,74.5 
            Q 21,72.5 14,50 
            L 24,71 
            C 28,45 29,30 24.5,5 Z"
            ></path>
        </svg>
    </div>
</section>

<script>
    import { gsap } from "gsap";
    import { SplitText, type SplitTextInstance } from "gsap/SplitText";

    document.fonts.ready.then(() => {
        gsap.registerPlugin(SplitText);
        const split = SplitText.create(".text", {
            type: "chars, words",
            onSplit: (self: SplitTextInstance) => {
                gsap.from(self.chars, {
                    yPercent: "random([-100, 100])",
                    rotation: "random(-30,30)",
                    ease: "back.out",
                    autoAlpha: 0,
                    yoyo: true,
                    stagger: {
                        amount: 0.5,
                        from: "random",
                        each: 0.05,
                    },
                    onComplete: () => {
                        split.revert();
                    },
                });
            },
        });
    });
</script>

<style>
    .accent-font {
        font-family: "Libre Baskerville", Georgia, serif;
        font-weight: 400;
    }
    .bold {
        font-weight: 900;
    }
    .color {
        color: #ff6600;
        color: #a4d21d;
    }

    h1 {
        font-family: "Libre Franklin", Arial, Helvetica, sans-serif;
        font-weight: 700;
        /* font-size: clamp(2rem, 6vw, 4rem); */
        font-size: 12vw;
        line-height: 1.2;
    }
    p {
        font-family: "Libre Franklin", Arial, Helvetica, sans-serif;
        font-weight: 400;
        font-size: clamp(1.25rem, 3.7vw, 2.25rem);
        font-size: 5vw;
        line-height: 1.5;
        color: #757575;
    }
    .hero {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        height: 80dvh;
        padding: 5rem 1rem 0 1rem;
        margin-bottom: 4rem;
        position: relative;
    }

    .projects-text {
        /* font-size: clamp(1.25rem, 3.5vw, 2rem); */
        font-size: 4vw;
        font-weight: 700;
        margin-bottom: 4rem;
        margin-top: 3rem;
        /* color: #333333; */
    }

    @media (min-width: 768px) {
        h1 {
            font-size: 5rem;
        }
        p {
            font-size: 2.25rem;
            max-width: 700px;
        }
        .projects-text {
            font-size: 1.75rem;
        }
    }
</style>
